apiVersion: v1
kind: Service
metadata:
  name: demo-api
  labels:
    app: demo-api
spec:
  type: NodePort
  selector:
    app: demo-api
    color: blue
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30090