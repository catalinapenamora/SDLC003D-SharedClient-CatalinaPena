# Misma convención que client.yaml → image-name: demo-api
# Sustituir YOUR_DOCKERHUB_USERNAME por tu usuario (igual que secret DOCKER_USERNAME).
# La receta deploy-api publica: <usuario>/demo-api:<tag> y :latest
#
# Borde HTTP: no duplicar HAProxy aquí. Aplicar primero en el cluster
# AUY1104-SharedWorkflows k8s-manifests/haproxy-academic/shared/
# (ea2-haproxy-academic). Ruta pública :30088 …/backend/* → este Service demo-api:3000.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-api
  labels:
    app: demo-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-api
  template:
    metadata:
      labels:
        app: demo-api
    spec:
      containers:
        - name: api
          image: asanchezoduoc/demo-api:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000
          # Sonda de disponibilidad: K8s espera que responda antes de enviarle usuarios
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 30  # Ajustado a 30s según las indicaciones
            periodSeconds: 10
            timeoutSeconds: 10       # Ajustado a 10s máximo de espera
          # Sonda de supervivencia: si tu app se congela, K8s la reinicia sola
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 30  # Ajustado a 30s según las indicaciones
            periodSeconds: 20
            timeoutSeconds: 10       # Ajustado a 10s máximo de espera